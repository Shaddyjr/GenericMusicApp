<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asc_guest.genericmusicapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ArtistActivity"
            android:label="@string/artistActivity_name"
            />
        <activity
            android:name=".GenreActivity"
            android:label="@string/genreActivity_name"
            />
        <activity
            android:name=".PlayingSongActivity"
            android:label="@string/playingSongActivity_name"
            />
        <activity
            android:name=".AllSongsActivity"
            android:label="@string/allSongsActivity"
            />
    </application>

</manifest>